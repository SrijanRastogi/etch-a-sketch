# Etch-a-Sketch

A browser-based Etch-a-Sketch drawing application built as part of [The Odin Project](https://www.theodinproject.com/) Foundations course.

## Features

- 16 × 16 grid generated dynamically with JavaScript.
- Change the grid size from 1 × 1 up to 100 × 100.
- Grid maintains the same overall drawing area when resized.
- Draw by hovering over the grid squares.
- Random RGB color generated for each square.
- Repeatedly hovering over a square progressively darkens its color.
- Uses Flexbox to create the grid layout.
- Input validation for grid size.

## Technologies Used

- HTML5
- CSS3
- JavaScript
- Flexbox
- DOM Manipulation

## What I Learned

- Creating and manipulating DOM elements with JavaScript.
- Using event listeners to handle user interactions.
- Using functions and parameters to avoid repeating code.
- Using loops to dynamically generate elements.
- Working with JavaScript closures and per-element state.
- Dynamically modifying CSS properties with JavaScript.
- Generating random RGB colors.
- Using Flexbox for layouts.
- Handling and validating user input.

## Project Structure

```text
Etch-a-Sketch/
├── index.html
├── styles.css
├── index.js
└── README.md